# CloakRFQ Backend

Off-ledger backend package for CloakRFQ.

This folder will hold ledger-client code that prepares workflow inputs, submits commands to Canton through supported APIs, and reads ledger state for application services.

The current folder is setup-only. It does not include transaction handlers, API endpoints, or generated Daml bindings yet.
